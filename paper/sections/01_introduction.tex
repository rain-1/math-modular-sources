\section{Introduction}\label{sec:intro}

An \emph{Ap\'ery system} for a real number $\xi$ is a pair of sequences $a_n\in\mathbb Z$, $b_n\in\mathbb Q$ satisfying one linear recurrence with polynomial coefficients, with
\[
a_n\asymp\lambda_1^n,\qquad |a_n\xi-b_n|\asymp\lambda_2^n,\qquad d_n^{\,k}b_n\in\mathbb Z,\qquad d_n=\operatorname{lcm}(1,\dots,n),
\]
and $\xi$ is irrational as soon as $\lambda_2e^k<1$. Ap\'ery's systems for $\zeta(2)$ and $\zeta(3)$ satisfy this with room to spare; in the forty years since, a large number of structurally similar systems have been found---Zagier's and Almkvist--Zudilin's sporadic families, Cooper's, the Ramanujan-Machine continued fractions, and many constructed from modular forms---and none of them does. This paper is about why.

Beukers' modular interpretation \cite{Beukers1987} supplies the three ingredients of such a system: a genus-zero group $\Gamma$, a Hauptmodul $t$ with integral $q$-expansion, and a modular form $f$ of weight $w$ with $\sum a_nt^n=f$. The companion $b$ is then an Eichler integral, and the special value $\xi$ is a period of the corresponding Eisenstein or cuspidal extension. Our starting point is that the three quantities in the inequality above are controlled by three \emph{separate} pieces of this data: the special value by the \emph{source} $\Phi=f\cdot Dt$ (Section~\ref{sec:sources}); the rates $\lambda_1,\lambda_2$ by the position of the cusps and elliptic points of $\Gamma$ in the coordinate $t$ (Section~\ref{sec:arch}); and the denominator depth $k$ by the number of integrations minus the number that are free. A fourth quantity, invisible in the inequality but decisive for what follows, is the $p$-adic behaviour of the ratios $b_n/a_n$ (Section~\ref{sec:padic}).

\subsection*{Results}

\emph{Sources (Section~\ref{sec:sources}).} For the twelve sporadic families with modular parametrisations the source $\Phi=f\cdot Dt$ is a holomorphic Eisenstein series with zero cuspidal projection (Theorem~A), and the Ap\'ery limit is \emph{exactly} the critical value: $\xi_\infty=L(\Phi,w+1)$ (Theorem~B$^*$), with no case-by-case prefactors---the census multiples $\tfrac16,\tfrac7{32},\tfrac58,\dots$ are read off the Mellin polynomial. Second-order rows reach their singularity at the cusp $0$, third-order rows at the Fricke fixed point; the three families with complex-conjugate folds have complex limits whose real part is $L(\Phi,w+1)$. Cuspidal sources give cusp-form $L$-values (two examples on the Domb curve), and Cooper's rows, whose sources are meromorphic, are shown to be weight-one rows in disguise: their free integration $(n+1)\mid A_n$ \emph{is} their source, $\Xi=\theta_q^{-1}\Phi$, and $\xi_\infty=L(\Xi,2)$ (Section~\ref{sec:weightdrop}). Period annihilation is finite Vandermonde algebra and produces new systems on demand.

\emph{Roots and an irrationality theorem (Sections~\ref{sec:sqrt}, \ref{sec:rootrows}).} Every third-order sporadic row is the symmetric square of a second-order row whose coefficients are integral after a $2$-power rescaling ($4^n[x^n]\sqrt G\in\mathbb Z$ for every $G\in1+x\mathbb Z[[x]]$; sharp $w$-th-root version $\lambda_w=w\cdot\mathrm{rad}(w)$), and the root row costs $k=2$ instead of $k=w+1$: Cooper's free integration, made general. Ap\'ery's own square root is a \emph{non-congruence} row ($\sqrt{F}$ has odd eta exponents and unbounded denominators), with positive score, and we prove that its period
\[
\xi=L(\Psi,2)=0.10018744922933940616\ldots,\qquad \Psi=\tfrac14\,(\eta_1\eta_6)^{9/2}(\eta_2\eta_3)^{-3/2},
\]
is irrational with $\mu(\xi)\le50.66$ (Theorem~\ref{thm:sqrt-irr}). This theorem is Beukers' Theorem~3 of 1987 \cite{Beukers1987}, which we rediscovered from the scan and recognised only afterwards; what is new is the framing as the symmetric-square root of Ap\'ery's recurrence, the explicit recurrence and Casoratian, the measure, and a formalization blueprint. No other root row has positive score; we explain why ($w\ge4$ meets interior zeros of $F$ or fractional exponents).

\emph{$p$-adic limits (Section~\ref{sec:padic}).} For a three-term row with constant term $c=\lambda_1\lambda_2$ the Casoratian is $-c^{n-1}/n^{w+1}$, so $b_n/a_n$ converges $p$-adically with slope $\sigma_p=v_p(c)+2\kappa_p$ (Proposition~C), and for integral rows $\sum_p\sigma_p\log p=\log|\lambda_1\lambda_2|$: archimedean imperfection is redistributed as $p$-adic convergence. A $p$-adic limit exists iff the Euler factor $1-\psi(p)p^{-s}$ divides the source polynomial (Theorem~F), and then $\xi_p=-Q(w+1)\kappa_p$ with $\kappa_p$ the Kubota--Leopoldt constant term of the $p$-adic Eisenstein series; cuspidal and outer-placement sources give $\xi_p=0$. Fourteen census identifications hold to about $3000$ $p$-adic digits, and the theorem is proved---by Calegari's overconvergence method---for Zagier's rows B, C, F at $p=3$ ($\xi_3=\tfrac12\zeta_3(2),\tfrac12\zeta_3(2),\tfrac58\zeta_3(2)$), the Domb/$\varepsilon$ pair at $p=2$, and the level-$16$ $\zeta(5)$ system; rows sharing a period have $p$-adic limits in the archimedean ratio corrected by the Euler factor (Conjecture~D, now a theorem in these cases). At good primes the extension splits along towers, the tower limits are $\Gamma_p$-assemblies carrying no $L$-value, and no rigidity holds---a trichotomy read off the tower Frobenius eigenvalue $(\psi(p)p^{-w},1,0)$ across $144$ cells.

\emph{Two-row constructions (Section~\ref{sec:tworow}).} For two aligned rows the quality of the correlated-lattice construction of \cite{ProjCatalanLCM,ProjCatalan58} has a closed form (Theorem~E) reproducing every number in the literature ($0.8579$, $0.9025$, $0.6589$, the formally verified $1-\varepsilon$ of \cite{CatalanLean}) and new ones (Domb$\times\varepsilon$ for $\zeta(3)$: $0.9010$). The common-period lemma behind the $0.9025$ Catalan construction is now a theorem on both halves (Theorem~F for the modular row; an exact relation to Beukers' Pad\'e system for Zudilin's row). A design rule explains why exactly Catalan and $\zeta(3)$ come within ten percent of the threshold and every other known pair---$L(2,\chi_{-3})$ with four aligned rows, $\zeta(5)$ with Brown--Zudilin's row---does not: the binding constraint is a \emph{decaying} partner with $p$-power denominators, which only hypergeometric rows supply.

\emph{Past the threshold (Section~\ref{sec:threshold}).} The Catalan construction is supercritical, yet nothing expressible through valuations---extra congruences, factorisation of the cross determinant, extra rows, directional divisibility, positivity, explicit moments---can exclude the rationality-kernel vector; the only remaining statement, Lemma~P2, is of irrationality strength, and a rational number with a $10^{320}$-digit denominator reproduces every table. The method thus converts Catalan's irrationality, without loss, into one statement about one explicit moment lattice.

\subsection*{What this paper claims, and what it does not}
No new irrationality theorem: the one irrationality statement we prove, for the non-congruence period $\xi=L(\Psi,2)$ of Section~\ref{sec:sqrt}, is Beukers' (1987). No claim about Catalan's constant, $\zeta(5)$, or any classical constant. Every displayed quality $\delta$ is below $1$ or accompanied by the control experiment showing a rational number produces the same numbers. Evidence grades are attached throughout: \Proved{} means a written proof or machine-checked certificate exists and is cited; \emph{verified} means an exact finite computation to a stated range; conjectures are labelled. The computations are reproducible from the scripts of Appendix~\ref{app:scripts}.

\subsection*{Provenance}
The results of Sections~\ref{sec:sources} and \ref{sec:arch} and the Catalan constructions were obtained in an extended AI-assisted research programme (GPT-5, Claude) directed by the first author; the project papers cited as \cite{ProjSources,ProjAnchors,ProjCatalanLCM,ProjCatalan58,LeanCatalan} contain the full proofs and certificates, and the Lean formalisation \cite{CatalanLean} of the $1-\varepsilon$ theorem depends only on the standard axioms. Sections~\ref{sec:padic}--\ref{sec:threshold} were obtained in the consolidation phase and are new.
